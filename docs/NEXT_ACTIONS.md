# 다음 할 일 (Next Actions) — 작업 이어가기

> 환경(회사↔집)을 옮겼을 때 "무엇을 하려던 참이었지?"를 즉시 복원하기 위한 메모다.
> `./sync-check.sh` 를 실행하면 이 내용이 자동으로 표시된다.
> **작업을 마칠 때마다 이 파일을 갱신한다.** (전체 히스토리·이유는 `DECISION_LOG.md`, 여기는 "지금/다음"만 짧게.)

## 📍 지금 상태
*마지막 업데이트: 2026-07-07 (회사 — 어제 세션 로드맵과 대조해 전체 백로그 통합)*

- ✅ 요금제·콘텐츠 등록 할당량 시스템 구현·검증·GitHub push 완료
- ✅ 커밋 작성자 자동 통일(`sync-check.sh`) 완료
- ✅ 의사결정 기록(`DECISION_LOG.md`) 도입
- ✅ **이메일 인증** 구현·검증 완료 — 미인증 로그인 차단, 재발송, 관리자 SMTP 설정(운영자 편집형)
- ✅ **플레이리스트 할당량 정책 결정** — "무제한(미적용)"으로 확정. 코드 변경 없음, 주석·문서 정비 (DECISION_LOG 참조)
- ✅ **온보딩 흐름** 구현·검증 완료 — 신규 사용자 환영 모달 + 대시보드 시작 체크리스트(3단계 자동 추적)
- 진행 중이던 미완 작업: **없음** (깨끗한 상태에서 다음 항목 시작 가능)

## ▶️ 다음에 이어서 할 일 (우선순위 순)

> 전체 백로그(보안·수익화·운영·성장 15+개)는 **`DECISION_LOG.md` 상단 "상용화 로드맵" 표**가 단일 출처.
> 여기는 그중 당장 집을 것만. (2026-07-07 통합 정리 — 어제 세션 로드맵과 대조해 누락분 반영)

**바로 할 수 있는 것 (외부 의존성 없음)**
1. [ ] **🔒 구글 Client Secret 재발급** — 지난 세션 중 노출된 시크릿 rotate (5분, 보안).
2. [ ] **비밀번호 재설정** — 기존 이메일 인프라(`email_service.py`, `app_settings`) 재사용.
       "비밀번호 찾기" → 재설정 토큰 메일 → 새 비밀번호. (빠르게 가능)
3. [ ] **AI 사용량 제한** — 서버 공용 키 비용 폭탄 방지(사용자별 일/월 AI 호출 한도).
4. [ ] **로그인 brute-force 방지 / 세션 쿠키 보안 플래그** — 빠른 보안 보강.

**결정·계정이 필요한 것**
5. [ ] **실결제(PG) 연동** — 먼저 결제사 선택: **토스페이먼츠(국내)** vs **Stripe(해외)**.
       결정 후 결제 성공 웹훅 → `set_user_plan()` 자동 부여. (현재는 관리자 수동 부여)

## ⚙️ 운영 준비 메모 (배포 시)
- 인증 메일을 실제로 보내려면: `/admin` → **이메일(SMTP) 발송 설정** 입력 후 "테스트 메일 발송"으로 확인.
- 설정 전에는 인증 메일이 안 나가고 링크가 서버 로그에만 남음(로컬은 화면에도 노출).

## 💡 다시 시작할 때 참고
- 왜 이렇게 만들었나(정책·철학·기각한 대안): `docs/DECISION_LOG.md`
- 상용화 로드맵 전체: `docs/DECISION_LOG.md` 상단 표 / `docs/ARCHITECTURE.md` 11.4
- 기능 명세: `docs/FEATURE_SPEC.md`
